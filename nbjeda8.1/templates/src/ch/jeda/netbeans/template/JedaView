<#if package?? && package != "">
package ${package};

</#if>
import ch.jeda.*;
import ch.jeda.event.*;
import ch.jeda.ui.*;

public class ${name} extends Program {

    private View view;

    @Override
    public void run() {
        view = new View();
    }
}
