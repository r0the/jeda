<#if package?? && package != "">
package ${package};

</#if>
import ch.jeda.*;
import ch.jeda.ui.*;

public class ${name} extends Simulation {

    @Override
    protected void init() {
    }

    @Override
    protected void step() {
    }
}
