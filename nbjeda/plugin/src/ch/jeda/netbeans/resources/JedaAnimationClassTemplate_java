<#if package?? && package != "">
package ${package};

</#if>
import ch.jeda.*;
import ch.jeda.event.*;
import ch.jeda.ui.*;

public class ${name} extends Program implements TickListener {

    private Window window;

    @Override
    public void run() {
        window = new Window();
        window.addEventListener(this);
    }

    @Override
    public void onTick(TickEvent event) {
    }
}
