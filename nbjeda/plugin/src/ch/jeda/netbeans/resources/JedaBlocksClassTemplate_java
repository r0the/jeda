<#if package?? && package != "">
package ${package};

</#if>
import ch.jeda.*;
import ch.jeda.blocks.*;
import ch.jeda.ui.*;

public class ${name} extends BlockWorld {

    @Override
    protected void init() {
    }

    @Override
    protected void update() {
    }
}
